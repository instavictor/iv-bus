{
  "name": "ivbus",
  "version": "1.0.0",
  "description": "Lightweight event bus implementation in Javascript",
  "main": "ivEventBus.js",
  "scripts": {
    "build": "npm run build:dev:legacy",
    "build:dev:legacy": "npm run clean && webpack -legacy",
    "build:dev": "npm run clean && webpack",
    "build:prod": "npm run clean && webpack -p",
    "build:prod:legacy": "npm run clean && webpack -p -legacy",
    "clean": "rm -rf dist",
    "debug": "",
    "test": "mocha --compilers js:babel-register -b"
  },
  "author": "Victor Wang",
  "license": "ISC",
  "dependencies": {
    "babel-core": "^6.13.2",
    "babel-loader": "^6.2.4",
    "babel-polyfill": "^6.13.0",
    "babel-preset-es2015": "^6.13.2",
    "babel-register": "^6.11.6",
    "gulp": "^3.9.1",
    "webpack": "^1.13.1"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "expect.js": "^0.3.1",
    "mocha": "^3.0.2"
  }
}
